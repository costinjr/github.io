# Shared demo passcode gating access to the whole app.
# Friction against casual visitors — not production authentication.
# Leave unset locally to skip the gate entirely.
DEMO_PASSCODE=

# Supabase Postgres connection string (Project Settings > Database > Connection string > URI).
# Server-only — never exposed to the client.
DATABASE_URL=
